# Supabase — LNBP Varonil
[supabase_config]
url = "https://YOUR_PROJECT.supabase.co"
anon_key = "YOUR_ANON_KEY"

# Contraseñas de acceso
[passwords]
# "usuario@email.com" = "contraseña"

# Roles de usuario: basic | all
[roles]
# "usuario@email.com" = "all"

# Entorno: omitir en producción, agregar para pruebas
# entorno = "test"
